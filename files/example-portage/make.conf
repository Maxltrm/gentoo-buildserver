# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
COMMON_FLAGS="-O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"

MAKEOPTS="-j12"
GRUB_PLATFORMS="pc"
# FLAGS GENERATED WITH cpuid2cpuflags
CPU_FLAGS_X86="aes avx f16c mmx mmxext pclmul popcnt sse sse2 sse3 sse4_1 sse4_2 sse4a ssse3"
USE="-nss gnome sasl gnutls ssl dri3 glamor savedconfig X mdadm airplay \
     bluray cec css dbus dvd gles lcms lirc nfs system-ffmpeg udev \
     upower gallium webserver xslt systemd networkmanager \
     pulseaudio udisks offensive bash-completion acl amd64 berkdb bzip2 \
     cli crypt cxx dri fortran gdbm iconv ipv6 libtirpc multilib \
     ncurses nls nptl openmp pcre readline seccomp tcpd unicode xattr \
     zlib alsa"
POLICY_TYPES="targeted"
ACCEPT_KEYWORDS="amd64"
INPUT_DEVICES="libinput"
VIDEO_CARDS="radeon"

ACCEPT_LICENSE="* -@EULA"

FEATURES="buildpkg"

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
#LC_MESSAGES=C

